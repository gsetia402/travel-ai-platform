APP_ENV=development
APP_PORT=8000
LOG_LEVEL=INFO
CORS_ORIGINS=*

GEMINI_API_KEY=
OPENWEATHER_API_KEY=

# Database — PostgreSQL for production, SQLite fallback for local dev
# Examples:
#   postgresql://user:password@host:5432/tripops    (Neon / Supabase / Render)
#   sqlite:///./travel_memory.db                     (local development)
DATABASE_URL=
