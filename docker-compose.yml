version: "3.9"

services:
  travel-ai:
    build: .
    container_name: travel-ai-platform
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - APP_ENV=production
    volumes:
      - ./travel_memory.db:/app/travel_memory.db
    restart: unless-stopped
